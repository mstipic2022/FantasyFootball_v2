spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:2024/fantasy_football
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
logging:
  level:
    footballfantasy.demo: debug
    root: DEBUG